# Dependencias para el proyecto ImageAnalysis
# Procesamiento de imágenes y visión por computadora
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.10.0
scikit-image>=0.20.0
Pillow>=10.0.0

# Visualización y gráficos
matplotlib>=3.7.0

# OCR (Reconocimiento de texto) - Deep Learning
easyocr>=1.7.0
torch>=2.0.0
torchvision>=0.15.0

# Dependencias adicionales para EasyOCR
python-bidi>=0.6.0
PyYAML>=6.0.0
Shapely>=2.0.0
pyclipper>=1.3.0
opencv-python-headless>=4.8.0

# Interfaz gráfica (Tkinter viene incluido con Python)
# No requiere instalación adicional

# Nota: EasyOCR descargará modelos automáticamente (~100MB) la primera vez
# Instalación: pip install -r requirements.txt
# Para PyTorch CPU (más ligero): pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
